import { useCallback } from 'react';

const useSortColors = () => {
    const handleColorChange = useCallback((color) => {
    }, [])

    return {
      handleColorChange,
    }
}

export default useSortColors;
